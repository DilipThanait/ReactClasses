const Newprops = (props) => {
  return (
    <div>
      <h1>{props.name}</h1>
      <h1>{props.age}</h1>
      {/* if bool value is comming and translated to string */}
    </div>
  );
};

// functionName.propTypes

export default Newprops;
